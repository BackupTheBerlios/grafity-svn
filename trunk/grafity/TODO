* do something when a function is not found
* auto zoom
* update legend on column rename
* cut/copy/paste in worksheets
* data reader
* screen reader

from grafity.extend import extension

@extension('column_tool')
@parameters(name='Differentiate', image='diff')
